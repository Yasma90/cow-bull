<UserControl x:Class="CowBullClient.View.MainControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignWidth="300" Height="30.718">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="67*"/>
            <ColumnDefinition Width="47*"/>
            <ColumnDefinition Width="31*"/>
            <ColumnDefinition Width="35*"/>
            <ColumnDefinition Width="29*"/>
            <ColumnDefinition Width="91*"/>
        </Grid.ColumnDefinitions>
        <TextBox HorizontalAlignment="Left" Height="26" Margin="11,2,0,0" TextWrapping="Wrap" Text="{Binding TBoxSendNumber}" VerticalAlignment="Top" Width="50" MaxLength="4"  Name="tboxNUmber"/>
        <Button Content="Send" HorizontalAlignment="Left" VerticalAlignment="Top" Background="Transparent" Width="41" Margin="2,3,0,0" Command="{Binding BtnSendCommand}" Height="25" IsEnabled="{Binding EnableButton}" Name="btnSend" RenderTransformOrigin="0.266,0.528" Grid.Column="1"/>
        <Label Content="{Binding TimeSound.TimerInput,Mode=OneWay}" IsEnabled="{Binding IsEnableTime}" FontSize="16" FontFamily="Comic" Foreground="Black" HorizontalAlignment="Left" Margin="4.126,4,0,0" VerticalAlignment="Top" Height="25" Width="77" Background="Transparent" RenderTransformOrigin="0.5,0.5" Grid.Column="5"/>
        <Button Name="buttonStartDict" ToolTip="Refresh" Command="{Binding }" Height="25" HorizontalAlignment="Left" VerticalAlignment="Top" Background="Transparent" Width="25" Margin="3,3,0,0" Grid.Column="2">
            <Button.Content>
                <Image Source="../Resources/ic_action_refresh.png" Stretch="Fill" SnapsToDevicePixels="True"/>
            </Button.Content>
            <!--<Button.Effect>
                <DropShadowEffect/>
            </Button.Effect>-->
        </Button>
        <Button Height="25" Width="30" Margin="2,3,0,0" Background="Transparent" HorizontalAlignment="Left" VerticalAlignment="Top" Grid.Column="3">
            <Button.Content>
                <Image Source="../Resources/ic_action_volume_on.png"/>
            </Button.Content>
        </Button>
    </Grid>
</UserControl>
