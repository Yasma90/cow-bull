<Window x:Class="CowBullServer.Modern.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="CowBull Server (Modern)" Height="500" Width="700"
        WindowStartupLocation="CenterScreen">
    
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <!-- Title -->
        <TextBlock Grid.Row="0" Text="CowBull Server - Modern Architecture" 
                   FontSize="20" FontWeight="Bold" 
                   HorizontalAlignment="Center" Margin="0,0,0,20"/>
        
        <!-- Server Status -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,10">
            <TextBlock Text="Status: " FontWeight="Bold"/>
            <TextBlock Text="{Binding ServerStatus}" Margin="5,0"/>
            <TextBlock Text="| Connected Clients: " FontWeight="Bold" Margin="20,0,0,0"/>
            <TextBlock Text="{Binding ConnectedClients}" Margin="5,0"/>
        </StackPanel>
        
        <!-- Control Buttons -->
        <StackPanel Grid.Row="2" Orientation="Horizontal" Margin="0,0,0,10">
            <Button Content="Start Server" Command="{Binding StartServerCommand}" 
                    Width="100" Height="30" Margin="0,0,10,0"/>
            <Button Content="Stop Server" Command="{Binding StopServerCommand}" 
                    Width="100" Height="30"/>
        </StackPanel>
        
        <!-- Log Messages -->
        <GroupBox Grid.Row="3" Header="Server Log" Margin="0,10,0,10">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <TextBlock Text="{Binding LogMessages}" 
                           FontFamily="Consolas" FontSize="12" 
                           Background="Black" Foreground="LightGreen"
                           Padding="5"/>
            </ScrollViewer>
        </GroupBox>
        
        <!-- Status Bar -->
        <StatusBar Grid.Row="4">
            <StatusBarItem>
                <TextBlock>
                    <Run Text="Ready"/>
                    <Run Text=" | Running: "/>
                    <Run Text="{Binding IsServerRunning}"/>
                </TextBlock>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>